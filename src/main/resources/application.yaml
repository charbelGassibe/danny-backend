spring:
  datasource:
    url: jdbc:mysql://localhost:3306/postulacion
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization=true:

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher


